<h1><%= @post.title %></h1>

<p><%= @post.body %></p>


<% if user_signed_in? %>
  <%= link_to 'Edit', edit_post_path(@post) %>
  <%= link_to 'Delete', post_path(@post), method: :delete, data: {confirm: 'are u sure?'} %>
<% end %>
